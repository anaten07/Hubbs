// app/(tabs)/logbook.tsx — Logbook (README §5.10). Placeholder until built.

import React from 'react';
import ComingSoon from '@/components/hobbs/ComingSoon';

export default function LogbookScreen() {
  return (
    <ComingSoon
      icon="book"
      label="Logbook"
      title="184.6 hours"
      blurb="A digital logbook — totals strip (month / landings / night / IFR), entry list with routes, tails, and who you split Hobbs with."
    />
  );
}
