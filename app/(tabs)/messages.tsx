// app/(tabs)/messages.tsx — Messages (README §5.9). Placeholder until built.

import React from 'react';
import ComingSoon from '@/components/hobbs/ComingSoon';

export default function MessagesScreen() {
  return (
    <ComingSoon
      icon="msg"
      label="Messages"
      title="Coordinate the flight"
      blurb="Thread list with unread badges, then a realtime conversation view to sort out FBO meet-ups, splits, and weather calls."
    />
  );
}
