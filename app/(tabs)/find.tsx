// app/(tabs)/find.tsx — Find a copilot (README §5.3). Placeholder until built.

import React from 'react';
import ComingSoon from '@/components/hobbs/ComingSoon';

export default function FindScreen() {
  return (
    <ComingSoon
      icon="compass"
      label="Find a copilot"
      title="The flight-share board"
      blurb="Filterable open listings — time-building, fly-outs, safety-pilot swaps — with pro-rata cost shares and FAR 61.113(c) notices."
    />
  );
}
