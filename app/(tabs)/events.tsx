// app/(tabs)/events.tsx — Events / fly-ins (README §5.7). Placeholder until built.

import React from 'react';
import ComingSoon from '@/components/hobbs/ComingSoon';

export default function EventsScreen() {
  return (
    <ComingSoon
      icon="cal"
      label="Events · fly-ins"
      title="Where pilots gather"
      blurb="Hangar nights to Oshkosh. Headliner covers, type filters, attendees from your circle, and fly-share seats to each event."
    />
  );
}
